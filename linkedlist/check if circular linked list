bool isCircular(Node *head)
{
  Node *first = head;
  while(first->next && first->next!=head)
  {
      first = first->next;
  };
  
  if(first->next == NULL)
  {
      return 0;
  }
  else 
       return 1;
}
